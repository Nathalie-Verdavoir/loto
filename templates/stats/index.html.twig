{% extends 'base.html.twig' %}

{% block title %}Hello StatsController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .chart-container {
            width: 100%;
            height: 400px
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Les Stats !</h1>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <div class="container d-flex">
<span class="col-lg-9 p-9">
        {{ render_chart(chart, {'class': 'ux-chart-line'}) }}

     </span>
        <span class="col-lg-3  p-3">

        {{ render_chart(chart2, {'class': 'ux-chart-do'}) }}
   </span>
    </div>
{% endblock %}
